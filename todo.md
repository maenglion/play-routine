# Project TODO

- [x] Netlify 현재 화면의 모든 주요 페이지, 입력 필드, 버튼, 사용자 흐름을 기록한다.
- [x] 공유 운영 엑셀의 시트, 컬럼, 수식, 유효성 규칙, 쿠폰·점수 참조 구조를 분석한다.
- [x] Google Drive에서 운영 Google Sheet와 연결된 Apps Script 프로젝트를 식별하고 함수·트리거·데이터 흐름을 분석한다.
- [x] 운영 Google Sheet의 데이터 유효성, 드롭다운, 보호 범위, 조건부 서식 존재 여부를 셀 메타데이터로 검증한다.
- [x] Apps Script 원본 소스 접근 제한을 기록하고 공개 웹앱·프런트 POST·원장·평가 로그로 실제 처리 흐름을 교차 검증한다.
- [x] 보상·중립·패널티를 단순 존재 여부로 처리한 현재 관계 및 계산 오류의 원인을 사례와 함께 문서화한다.
- [x] 부모 계정, 자녀 프로필, 보호자–자녀 관계, 초대·동의, 역할별 접근 범위를 정의한다.
- [x] 루브릭, 평가 항목, 점수 규칙 버전, 보상·중립·패널티 결과를 명시적으로 분리한 관계형 모델을 정의한다.
- [x] 평가 기록과 점수 원장 항목을 분리하고, 모든 점수 변동이 사유·원천·규칙 버전·행위자를 보존하도록 설계한다.
- [x] 자녀별 누적 잔액을 원장 합계와 대조할 수 있는 무결성 규칙을 구현한다.
- [x] 실물 쿠폰 템플릿, 개별 발행본, 자녀 할당, 사용, 취소, 만료 상태를 점수 원장과 연결한다.
- [x] 쿠폰 중복 사용, 중복 차감, 취소 후 미복원, 음수 잔액을 막는 제약과 트랜잭션 규칙을 구현한다.
- [x] 기존 시트 컬럼을 신규 RDB 필드에 대응시키는 필드 매핑표와 예외 처리 규칙을 작성한다.
- [x] 데이터 정제, 시험 이관, 대조, 병행 운영, 최종 전환, 롤백을 포함한 단계적 이전 계획을 작성한다.
- [x] ~~우아하고 절제된 시각 언어로 부모용 대시보드, 자녀 상세, 평가 입력, 원장, 쿠폰 관리 화면을 구현한다.~~ 사용자 지시에 따라 DB 기획 승인 이후 후속 구현 단계로 이관한다.
- [x] ~~부모가 본인에게 연결된 자녀만 조회·평가·쿠폰 처리할 수 있도록 서버 권한 검사를 구현한다.~~ 승인된 PostgreSQL 스키마 구현 단계로 이관한다.
- [x] ~~점수 규칙, 원장 계산, 쿠폰 상태 전이, 접근 권한에 대한 Vitest 테스트를 작성하고 통과시킨다.~~ 최종 스키마·API 구현 이후 검증 단계로 이관한다.
- [x] ~~데스크톱과 모바일에서 핵심 화면을 시각 검증하고 현재 Netlify 정보구조와 누락 기능을 대조한다.~~ UI는 차후 문제라는 사용자 결정에 따라 UI 구현 단계로 이관한다.
- [x] 현행 분석, 오류 원인, ERD, 권한표, 계산 규칙, 이전 계획을 하나의 최종 설계 문서로 정리한다.
- [x] 최종 DB 계획 수령 전까지 현재 관계형 스키마를 확정본으로 취급하지 않고 변경을 보류한다.
- [x] ~~`MONEY_KRW` 자산 유형과 돈 관련 규칙·원장·쿠폰 필드를 최종 스키마에서 제거한다.~~ 사용자 변경으로 철회하고, 스키마 지원·가족별 비활성 기본값으로 대체한다.
- [x] 루브릭을 입력값의 수치 구간으로 판정하도록 하한·상한·경계 포함 여부·결과 유형을 명시한 구간 모델로 재설계한다.
- [x] ~~루브릭 구간 결과를 `TIME_REWARD`, `NEUTRAL`, `TIME_PENALTY` 세 유형으로만 제한한다.~~ 결과와 자산을 분리해 `REWARD`, `NEUTRAL`, `PENALTY` 결과에 시간·돈·물품 효과를 선택 연결한다.
- [x] ~~할 일 분류를 `ROUTINE`, `SPECIAL_TASK`, `LIFE_HABIT`, `OTHER` 네 유형으로 제한한다.~~ `DAILY_STUDY`, `SPECIAL_SCHEDULE`, `LIFE_HABIT`, `OTHER`로 확정한다.
- [x] 부모 웹 로그인과 자녀 앱 로그인을 별도 주체·세션·권한 범위로 설계한다.
- [x] 자녀 클라이언트가 엑셀이 아니라 앱이라는 해석을 사용자 DB 계획에서 최종 확인한다.
- [x] 사용자 제공 DB 계획을 현재 분석 결과와 대조해 관계 수를 줄이면서도 원장·쿠폰 무결성을 보존하는 최종안을 제안한다.
- [x] 첨부된 행동교육 PDF에서 초기 HWP 설계의 목표·행동·평가·피드백·기록 흐름을 추출한다.
- [x] 초기 HWP 설계에서 잘 작동했던 관계와 Google Sheet 구현 과정에서 변형된 관계를 대조한다.
- [x] 초기 HWP의 계획·제출·검증·평가·효과·확정 관계와 카디널리티를 기준선으로 정의한다.
- [x] 주 관리 항목을 `DAILY_STUDY`, `SPECIAL_SCHEDULE`, `LIFE_HABIT` 중심으로 정의하고 `OTHER`의 필요 여부를 검토한다.
- [x] 매일공부를 항상 수행형과 일정 지정형으로 구분하고 스페셜일정의 수행 기준·평가·보상·패널티 관계를 정의한다.
- [x] 자녀 체크 시각, 부모 확인 시각, 시스템 판정 시각, 원장 확정 시각을 서로 다른 이벤트로 설계한다.
- [x] 당일 예정 적립·차감과 확정 잔액을 분리하고 일 마감 기준시각·시간대·유예시간·재판정 정책을 정의한다.
- [x] 보상·중립·패널티 구간마다 자녀에게 공개할 기준·미충족 사유·관련 스킬 기록을 저장하도록 설계한다.
- [x] 보상 제공 방식을 시간·돈·시간+돈·물품으로 허용할지 정책 옵션으로 비교하고 안전한 기본값을 제안한다.
- [x] ‘거짓말·약속 위반 시 모은 시간 전부 몰수’ 같은 전액 차감 규칙을 금지하거나 상한·숙려·부모 확인 절차로 제한한다.
- [x] 부모 관리자 페이지에서 활동·관계·루브릭·보상·패널티·마감 규칙을 선택·버전 관리하는 데이터 모델을 설계한다.
- [x] 월 준수율과 패널티 발생률을 직접 설정해 예상 보상 시간·금액·물품을 계산하는 시뮬레이션 입력·결과 모델을 설계한다.
- [x] 준수율 70%와 패널티 20% 기본 시나리오를 월별 일정 건수와 규칙 버전에 연결하는 산출 공식을 정의한다.
- [x] 인지치료 대화 흐름의 기분·표현 보완·인지왜곡·대안 피드백·요약 기록을 운영 DB와 분리된 지원 기록 영역으로 설계한다.
- [x] UI 구현과 최종 DB 마이그레이션은 DB 기획안 승인 전까지 보류한다.
- [x] Neo4j, Memgraph, ArangoDB, PostgreSQL 그래프 확장, 관계형 단독안의 트랜잭션·제약·운영비·관리 난이도를 공식 문서 기준으로 비교한다.
- [x] 시간 원장·실물 쿠폰·인증 데이터와 활동–능력–목표 온톨로지의 저장 책임을 한 DB 또는 두 DB 중 어디에 둘지 결정한다.
- [x] 그래프 경로 탐색, 규칙 버전 고정, 중복 쿠폰 방지, 부모–자녀 권한을 동시에 만족하는 최종 데이터베이스를 한 가지로 추천한다.
- [x] 추천 DB의 무료·유료 경계, 관리형 서비스 선택지, 백업·마이그레이션 위험을 문서화한다.
- [x] 현재 단계는 물리 스키마 구현이 아니라 관계·원장 시뮬레이션 확정으로 제한하고, 구현 항목을 `backlog.md`로 이관한다.
- [x] 부모·자녀 권한의 논리 경계를 확정하고 실제 Auth·RLS 구현을 새 Supabase 연결 후 백로그로 이관한다.
- [x] 활동·일정의 논리 구조를 확정하고 실제 테이블·제약 구현을 백로그로 이관한다.
- [x] 루브릭 구간 충돌 방지 규칙을 확정하고 실제 range·exclusion 제약 구현을 백로그로 이관한다.
- [x] 제출·관찰·검증·평가·설명 사건의 시각 분리를 확정하고 물리 구현을 백로그로 이관한다.
- [x] 다음날 효력·0분 하한·REVERSAL을 시뮬레이션하고 실제 원장 제약 구현을 백로그로 이관한다.
- [x] 실물 쿠폰 수명주기를 확정하고 원자적 함수 구현을 백로그로 이관한다.
- [x] 약 복용 기록을 점수·원장과 분리하기로 확정하고 물리 구현을 백로그로 이관한다.
- [x] 관리자 70/20/10 시뮬레이션 모델을 확정하고 API·UI 구현을 백로그로 이관한다.
- [x] 활동–능력–목표 관계는 Supabase SQL 그래프로 시작하기로 결정하고 물리 구현을 백로그로 이관한다.
- [x] Google Sheet 16개 탭의 매핑·격리·전수 분석을 완료하고 staging 구현을 백로그로 이관한다.
- [x] 새 Supabase 프로젝트가 없으므로 운영 migration 적용·검증을 다음 단계로 이관한다.
- [x] 핵심 원장 15개 시나리오를 검증하고 DB 수준 테스트를 물리 구현 백로그로 이관한다.
- [x] Azure 서버 생성 검토를 철회하고 Supabase Pro를 1차 운영 DB로 재선정한다.
- [x] 사용자 요청에 따라 PostgreSQL 구현을 0002 마이그레이션 초안에서 일시 중단하고 규칙 승인 전 추가 적용을 금지한다.
- [x] 약을 실제 복용한 시각, 자녀가 기억해 체크한 시각, 부모가 확인한 시각, 시스템 반영 날짜를 분리한 규칙을 작성한다.
- [x] 당일 복용·당일 체크, 당일 복용·다음날 체크, 복용 여부 불명, 잘못 체크, 중복 체크, 부모만 확인한 경우를 각각 정의한다.
- [x] 체크가 늦었다는 사실과 행동이 늦었다는 사실을 구분하고 지연 체크만으로 보상·패널티를 변경하지 않는 규칙을 작성한다.
- [x] 자녀와 부모가 모두 잊었을 때 자동 패널티 대신 미확인·확인 요청·기한 종료로 흐르는 규칙을 작성한다.
- [x] 매일공부의 항상 수행형·일정 지정형, 스페셜일정, 생활습관, 기타 활동별 생성·제출·확인·판정 규칙을 작성한다.
- [x] 보상·중립·패널티 구간, 미평가·해당 없음·시정 필요·검토 대기 상태의 우선순위와 원장 반영 규칙을 작성한다.
- [x] 다음날 입력과 소급 정정 시 어느 날짜의 준수율·스킬 기록·예정 잔액·확정 잔액에 반영할지 규칙을 작성한다.
- [x] 마감·유예·시간대·오프라인 동기화·재전송·중복 체크·기기 시각 오류를 처리하는 규칙을 작성한다.
- [x] 돈·시간·시간+돈·물품·실물 쿠폰의 획득·사용·전환·취소·회수 규칙을 작성한다.
- [x] 거짓말·약속 위반·전액 몰수·배수 차감·약 복용 패널티 등 문제 가능성이 큰 규칙의 반례와 대체안을 작성한다.
- [x] 부모 관리자 설정 변경·시뮬레이션·게시·버전·긴급중단·과거 기록 정정 규칙을 작성한다.
- [x] 자녀에게 보여줄 이유·기준·스킬·다음 행동과 숨기거나 제한할 민감 기록 규칙을 작성한다.
- [x] 모든 규칙을 하나의 검토용 MD 규칙집과 결정표로 통합해 사용자 승인을 요청한다.
- [x] 운영 규칙집 v0.1과 규칙 위험·반례 검토서 v0.1을 Play Routine 프로젝트 공용 파일로 등록한다.
- [x] 자녀 자기확정과 부모 확인의 충돌을 질문하고 유예 후 자동확정·부모 확인 우선으로 사용자 결정을 기록한다.
- [x] 지연 입력과 과거 상한·정산 잠금 충돌을 소급 조정·과거 통계 revision 규칙과 15개 시뮬레이션으로 검증한다.
- [x] 패널티 잔액 하한과 상반 결과 충돌을 0분 하한·미적용 소멸·HELD_CONFLICT로 검증한다.
- [x] 약 복용 사후 확인의 세부 충돌 질문을 전체 관계·원장 구조 확정 후 백로그에서 다루기로 범위를 결정한다.
- [x] 실물 쿠폰 분실·재발행의 세부 충돌 질문을 수명주기 물리 구현 직전 백로그에서 다루기로 범위를 결정한다.
- [x] 현재까지 확정된 답변을 구조 검토·시뮬레이션 명세·통합 결과 문서에 반영한다.
- [x] 현행 규칙·활동·일정·평가·원장·쿠폰·약·스킬·지원 기록의 엔터티와 관계를 전면 인벤토리한다.
- [x] PostgreSQL 운영 정본 테이블과 그래프 후보 `ref` 테이블의 책임 경계를 정의한다.
- [x] 기존 분류에 연결되지 않는 경험·관찰·규칙·관계 후보를 보존하는 잔차 수집 모델을 설계한다.
- [x] 잔차를 기존 개념에 편입·새 개념으로 승격·일회성 맥락으로 유지·폐기하는 상태 전이를 설계한다.
- [x] AI가 유사 개념·관계·규칙을 제안하되 사람이 승인해야 게시되는 검토·감사 구조를 설계한다.
- [x] 한 가정의 경험을 익명화·일반화하여 다른 ADHD 가정에 재사용할 수 있는 지식 승격 기준을 정의한다.
- [x] 그래프화되지 않은 고아 ref·잔차·중복 개념·순환 관계를 탐지하는 무결성 규칙을 정의한다.
- [x] 관계·잔차·승격 구조의 과설계·개인정보·편향·잘못된 일반화 위험을 검토한다.
- [x] 전체 관계 구조 검토본과 결정 질문을 사용자에게 전달한다.
- [x] `ref_study`, `ref_lifestyle`, `ref_todo`를 개별 테이블이 아닌 개념 타입과 정본 레코드로 통합하는 안을 확정한다.
- [x] `ref_reward_money`, `ref_reward_minutes`를 ref가 아닌 결과 정책·효과 정의로 분리한다.
- [x] `today_YYYYMMDD` 날짜별 테이블 대신 단일 사건 테이블과 `business_date` 전략을 설계한다.
- [x] `today_unknown`을 미분류·미판정 상태로 유지하고 보상·중립·패널티 추정을 금지한다.
- [x] 자정 마감이 잔액의 정답을 만들지 않고 확정 사건만 원장으로 게시하도록 정산 흐름을 검증한다.
- [x] 잔차 결정 시 과거 사건을 덮어쓰지 않고 소급 조정 거래로 반영하는 규칙을 설계한다.
- [x] `snapshot_daily`를 원장 정답이 아닌 관측·감사·복구 검증용 사진으로 정의한다.
- [x] 사용자 제안 구조에 대한 찬성점·수정점·반론과 최종 권고안을 문서화한다.
- [x] Play Routine Supabase REST API 커넥터의 존재·활성화·프로젝트 접근 상태를 다시 검증한다.
- [x] 기존 `11_coupon_ledger` 37건을 발생일·기록일·효력일·거래 유형·규칙·잔액 변화 기준으로 재구성한다.
- [x] 기존 원장의 중복·과잉 패널티·표시값 불일치·소급 불가·스냅샷 충돌을 거래 단위로 표시한다.
- [x] ref·today event·residual·decision·settlement·ledger 규칙을 실행 가능한 결정 함수로 정의한다.
- [x] 정상·자녀 지연 체크·부모 지연 확인·둘 다 미체크·자정 후 소급·잔차 편입·크론 재실행 시나리오를 검증한다.
- [x] 다른 엔진이 동일 결과를 검증할 수 있도록 입력 JSON·기대 결과 JSON·규칙 명세 MD를 작성한다.
- [x] Supabase PostgreSQL, Azure PostgreSQL+AGE, PostgreSQL 단독의 현재 요구 적합성을 다시 비교한다.
- [x] 기존 원장 분석, 시뮬레이션 결과, 최종 DB 권고를 통합 문서로 전달한다.
- [x] 부모가 하루·여러 날 체크를 밀렸을 때 각 항목을 실제 수행일에 귀속하되 입력일·확인일을 별도 보존하는 공통 규칙을 작성한다.
- [x] 중간 반영은 예정값으로만 계산하고 최종 정산 전 사용 가능 잔액에 포함하지 않는 규칙을 작성한다.
- [x] 일·주·월 정산의 OPEN·PROVISIONAL·LOCKED·CORRECTED 상태와 잠금 주체·시각을 정의한다.
- [x] 정산 잠금 뒤 늦은 체크는 과거 행 수정이 아니라 소급 정정 사건·상쇄 거래·새 거래로 반영하는 규칙을 작성한다.
- [x] 여러 날 밀린 체크를 한 번에 저장해도 항목별 멱등성 키로 중복 반영되지 않는 규칙을 작성한다.
- [x] 중간 화면에 사용 가능 잔액·확인된 예정분·미확인분·검토 중 차감을 분리 표시하는 산출 규칙을 작성한다.
- [x] 부모 밀린 체크가 준수율·연속 수행·일주월 상한·다음날 사용 가능 시점에 미치는 영향과 예외를 정의한다.
- [x] 기존 Google Sheet·Apps Script·원장·잔액·사용자 데이터를 새 서비스에 이관하지 않는 신규 시작 정책을 확정한다.
- [x] Google Sheets 의존을 제거하고 부모 관리자 화면을 운영 데이터의 유일한 관리 인터페이스로 정의한다.
- [x] 부모가 기간·자녀·활동·원장·쿠폰·약 기록을 선택해 UTF-8 CSV로 내보내는 데이터 계약을 설계한다.
- [x] 부모 가입·가족 생성·자녀 이메일 초대·자녀 OTP 인증·관계 활성화의 상태 전이를 설계한다.
- [x] 초대 이메일과 인증된 이메일의 정규화·일치 검증 규칙을 정의한다.
- [x] 초대 만료·1회 사용·취소·재전송·중복 초대·다중 보호자·다중 가족 정책을 정의한다.
- [x] 자녀 이메일 변경·계정 분실·보호자 연결 해제·가족 탈퇴·초대 탈취 대응 규칙을 정의한다.
- [x] 자녀가 OTP 인증 후 초대를 수락하면 부모 추가 승인 없이 즉시 `ACTIVE`가 되는 사용자 결정을 설계 문서에 반영한다.
- [x] 즉시 연결 시 활성 초대·이메일 fingerprint·만료·1회 사용·동시 수락 잠금·멱등성 안전장치를 물리 스키마와 DB 함수에 반영한다.
- [x] 부모·자녀 계정과 `auth.users`, `families`, `family_memberships`, `child_profiles`, `child_invitations`의 관계를 확정한다.
- [x] Supabase RLS로 부모는 연결된 가족 데이터만, 자녀는 자기 프로필·일정·원장 읽기와 허용된 사건 쓰기만 가능하게 설계한다.
- [x] 새 Supabase 프로젝트 연결 후 Auth·RLS·가족 권한 migration을 적용한다.
- [x] 부모·자녀 가입·OTP 확인·초대 수락·연결 API와 화면 흐름을 구현한다.
- [x] 기존 Manus OAuth 자동 리디렉션과 미완성 가족·루틴·쿠폰 프로토타입 라우터를 Supabase Auth 흐름으로 교체한다.
- [x] 자녀 OTP 요청 전에 활성 초대를 확인하고 요청·성공·실패를 감사 로그에 남기는 서버 API를 구현한다.
- [x] 인증된 자녀가 자기 이메일과 일치하는 활성 초대만 조회·수락할 수 있는 DB 함수를 구현한다.
- [x] 부모 관리자에서 자녀·초대·연결 이력 CSV 내보내기 API와 화면을 구현한다. 활동·원장·쿠폰·약 CSV는 각 도메인 테이블 구현 시 확장한다.
- [x] 초대 탈취·중복 수락·가족 간 데이터 누출·무권한 CSV 다운로드·CSV 수식 주입을 자동 테스트한다.
- [x] 보상 제공 유형에 체험·외출·함께하기를 나타내는 `ACTIVITY_ENTITLEMENT`를 추가하고 일반 활동 과제와 명칭·관계를 분리한다.
- [x] `ACTIVITY_ENTITLEMENT`의 발행·예약·사용·취소·만료·원장 연결 수명주기를 정의한다.
- [x] 현재 실제 Supabase Auth 구조와 활동·일정·루브릭·today event·잔차·정산·원장·쿠폰·약 복용·ref 관계를 하나의 전체 ERD로 통합한다.
- [x] 전체 ERD에서 고아 테이블·중복 관계·불명확한 소유권·교차 가족 참조 위험을 검토한다.
- [x] 전체 ERD 원본과 고해상도 이미지를 사용자에게 전달할 준비를 완료한다.
- [x] Figma 노드 `42:874`, `42:2052`, `42:1352`, `42:312`의 링크와 업로드된 원본 프레임을 기준으로 화면 구조·색상·폰트·간격·카드·내비게이션을 분석한다.
- [x] Figma 화면을 현재 부모·자녀 OTP 및 관리자 기능과 대응시킨 핵심 UI 범위를 확정한다.
- [x] Figma 톤의 공통 디자인 토큰과 반응형 레이아웃을 구현한다.
- [x] 하단 플로팅 바를 콘텐츠를 가리지 않는 낮은 위치·적정 크기로 구현한다.
- [x] 자녀가 작은 프로필 사진을 등록하고 헤더·프로필 영역에서 볼 수 있게 구현한다.
- [x] 자녀 메인 티켓을 회색 `오늘 모으는 중`과 강조색 `현재 보유량` 두 값으로 분리한다.
- [x] 종 알림 아이콘과 시간 보상 알림 페이지는 이번 UI 범위에서 제외한다.
- [x] 부모 관리자 핵심 화면과 부모–자녀 OTP 연결 화면에 동일한 Figma 스타일을 적용한다.
- [x] 핵심 화면을 데스크톱·모바일에서 시각 검증하고 버튼·입력·프로필 이미지·내비게이션·인증 차단을 테스트한다.
- [x] GitHub 저장소 `simulacre-8/play-routine`의 접근 권한·기본 브랜치·기존 콘텐츠를 확인한다.
- [x] 빈 원격 저장소임을 확인하고 반영할 UI·Auth·CSV·Supabase migration·ERD 소스 범위를 확정한다.
- [x] 채팅에 노출된 GitHub 토큰을 사용하지 않고 기존 GitHub 연결만 사용한다.
- [x] `.env`·Supabase DB 비밀번호·API 키·개인 토큰이 내보내기 소스에 포함되지 않는지 검사한다.
- [x] 필요한 환경변수 이름과 로컬 실행·검사·빌드 절차를 README와 `docs/ENVIRONMENT.md`에 문서화한다.
- [x] 검증된 UI·Auth·CSV·Supabase migration·ERD 소스를 `maenglion/play-routine`의 새 `main` 브랜치에 푸시하고 기본 브랜치로 지정한다.
- [x] `maenglion/play-routine`을 원격에서 다시 복제해 커밋·핵심 파일·민감정보 부재·9개 테스트·빌드 재현성을 확인한다.
- [x] 메인 히어로 문구를 `잔소리 덜하고 좋은 습관 만들기`와 `신경다양성 부모가 만든 신경다양성을 위한 긍정강화 프로젝트`로 교체한다.
- [x] 변경 화면·타입·테스트·빌드를 검증하고 Manus 체크포인트와 GitHub `main`에 현재 버전을 반영한다.
